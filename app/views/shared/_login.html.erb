<!--<div class="panel radius small">Log In</div>
<% flash.each do |key, msg| %>
  <%= content_tag :div, msg, :id => key %>
<% end %>-->
<%= form_tag sessions_path do %>
  <div class="field">
    <%= label_tag :email %><br />
    <%= text_field_tag :email, params[:email] %>
  </div>
  <div class="field">
    <%= label_tag :password %><br />
    <%= password_field_tag :password %>
  </div>
  <div class="actions"><%= submit_tag "Log In", :class =>"small button" %></div>
<% end %>

